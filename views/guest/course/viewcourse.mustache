<html>
<head>
{{>partials/header}}
</head>

<body class="container-fluid py-5 bg-light mt-4">
<div class="container">
        <h1 class="mb-5 text-center fw-light">{{title}}</h1>

        {{#entries}}
        <div class="card mb-4 shadow-sm ">
            <div class="card-body">
            <div class="d-flex">
                <div class="m-1">
                    <h3 class="card-title fw-semibold mb-3">{{name}}</h3>
                </div>
                
            </div>

            <div class="">
                    <div class="d-flex">
                        <p>Duration:</p>
                        <p>{{duration}}</p>
                        <p>hours</p>
                    </div>
                </div>
                <div class="d-flex ">
                    <a href="/joincourse/{{_id}}" class="btn btn-primary btn-md">Join </a>
                    <a href="/viewclasses/{{_id}}" class="btn btn-primary">Show Classes </a>

                    
                </div>
            </div>
        </div>
        {{/entries}}

    </div>
<div>

</div>
</body>
</html>

